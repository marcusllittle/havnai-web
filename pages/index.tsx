// Temporary: make the site root use the same generator
// experience as /test so joinhavn.io does not 404.
// We can later replace this with a dedicated landing page
// component that mirrors the original static index.html.
export { default } from "./test";

